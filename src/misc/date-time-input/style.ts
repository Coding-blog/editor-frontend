import { style } from '../../util/style';

export const classes = style({
  dateTimeWidget: {
    display: 'flex',
    gap: 8,
  },
  divider: {
    width: 1,
    background: 'rgba(255, 255, 255, .1)',
  }
});
