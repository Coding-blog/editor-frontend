name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install TyFON
        run: sudo npm i -g tyfon

      - name: Checkout
        uses: actions/checkout@v2

      - name: Run ESLint
        run: eslint . --ext .js,.jsx,.ts,.tsx
